// TODO: after writing standard time document. add new component tags and add them to components already using related standard timing

// milliseconds
export const FADE_IN_DURATION = 400
/**
 * The maximum delay between key strokes while typing a text in ms. Mainly used to defer (debounce) triggering filtering method while typing is taking place
 */
export const KEY_STROKE_DELAY = 300
// milliseconds
export const MOUSE_ACTION_DELAY = 50
// seconds
export const LOADING_ANIMATION_CYCLE = 1
// milliseconds
export const SMALL_CONTENT_EXPAND_COLLAPSE_DURATION = 300
